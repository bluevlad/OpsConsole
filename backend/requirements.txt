# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0       # pydantic EmailStr 의존

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
google-auth==2.35.0
requests==2.32.3              # google.auth.transport.requests 의존

# --- HTTP client ---
httpx==0.27.2

# --- Scheduler (P1 헬스 잡) ---
apscheduler==3.10.4

# --- YAML / Manifest ---
pyyaml==6.0.2

# --- Test ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.23.1                 # httpx 모킹 (fetcher 테스트)
